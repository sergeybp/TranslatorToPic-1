<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="translator.BudkovMaksakovaMarkovnikov.ru.translator">
    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">
        <activity
            android:name="translator.BudkovMaksakovaMarkovnikov.ru.translator.InputActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>5
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name="translator.BudkovMaksakovaMarkovnikov.ru.translator.DrawItActivity"
            android:label="Have a good time"/>

        <activity
            android:screenOrientation="portrait"
            android:name="translator.BudkovMaksakovaMarkovnikov.ru.translator.TranslationActivity"
            />

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
